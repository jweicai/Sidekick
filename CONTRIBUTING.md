# 贡献指南

感谢你对 Sidekick 项目的关注！我们欢迎任何形式的贡献。

## 如何贡献

### 报告 Bug

如果你发现了 Bug，请：

1. 在 [Issues](https://github.com/your-username/Sidekick/issues) 中搜索是否已有相关问题
2. 如果没有，创建一个新的 Issue，包含：
   - 问题描述
   - 复现步骤
   - 预期行为
   - 实际行为
   - 系统环境（macOS 版本、应用版本）
   - 截图（如果适用）

### 提交功能建议

我们欢迎新功能建议！请：

1. 在 Issues 中创建一个 Feature Request
2. 清楚地描述你的需求和使用场景
3. 如果可能，提供设计方案或实现思路

### 提交代码

#### 开发环境

- macOS 14.0 或更高版本
- Xcode 15.0 或更高版本
- Swift 5.9 或更高版本

#### 步骤

1. **Fork 项目**

```bash
# 克隆你的 fork
git clone https://github.com/your-username/Sidekick.git
cd Sidekick
```

2. **创建分支**

```bash
git checkout -b feature/your-feature-name
# 或
git checkout -b fix/your-bug-fix
```

3. **开发和测试**

```bash
# 构建项目
swift build

# 运行测试
swift test

# 使用 Xcode 运行应用
open Package.swift
```

4. **提交代码**

```bash
git add .
git commit -m "feat: add your feature description"
# 或
git commit -m "fix: fix your bug description"
```

提交信息格式：
- `feat: 新功能`
- `fix: Bug 修复`
- `docs: 文档更新`
- `style: 代码格式调整`
- `refactor: 代码重构`
- `test: 测试相关`
- `chore: 构建/工具相关`

5. **推送并创建 Pull Request**

```bash
git push origin feature/your-feature-name
```

然后在 GitHub 上创建 Pull Request。

#### Pull Request 要求

- 清晰的 PR 描述
- 相关的 Issue 引用（如果有）
- 通过所有测试
- 遵循项目代码风格
- 添加必要的文档

### 代码风格

- 遵循 Swift 官方代码风格
- 使用 4 空格缩进
- 添加必要的注释
- 保持代码简洁可读

### 项目结构

```
Sidekick/
├── Sources/
│   ├── App/              # 应用入口
│   ├── Models/           # 数据模型
│   ├── Views/            # UI 视图
│   ├── ViewModels/       # 视图模型
│   ├── Services/         # 业务逻辑
│   │   ├── FileLoader/  # 文件加载
│   │   ├── SQLEngine/   # SQL 引擎
│   │   └── Tools/       # 工具服务
│   └── Utilities/        # 工具类
├── Tests/                # 测试
├── docs/                 # 文档
└── sample_data/          # 示例数据
```

## 开发指南

### 添加新的文件格式支持

1. 在 `Sources/Services/FileLoader/` 创建新的 Loader
2. 实现 `FileLoaderProtocol` 协议
3. 在 `FileLoaderManager` 中注册
4. 添加对应的测试

### 添加新的工具功能

1. 在 `Sources/Services/Tools/` 创建服务类
2. 在 `Sources/Views/Tools/` 创建视图
3. 在 `MainView.swift` 中注册
4. 更新文档

### 运行测试

```bash
# 运行所有测试
swift test

# 运行特定测试
swift test --filter testName
```

## 社区准则

请阅读并遵守我们的 [行为准则](CODE_OF_CONDUCT.md)。

## 许可证

通过贡献代码，你同意你的贡献将以 MIT 许可证发布。

## 联系方式

如有问题，可以通过以下方式联系：

- GitHub Issues
- GitHub Discussions
- Email: [添加项目邮箱]

感谢你的贡献！🎉
